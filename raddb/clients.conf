# Add the RADIUS clients to the bottom of the file by specifying their subnet:
 client 192.168.1.0/24 {
         ipaddr          = 192.168.1.0/24
         secret          = RADIUS_secret           # EDIT - used on the APs in the hostapd config
         shortname       = wifi_APs
 }
